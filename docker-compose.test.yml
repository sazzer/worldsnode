version: '3.2'
services:
    database:
        build: ./database
    service:
        build: ./service
    testing:
        build: ./testing
        volumes:
            - type: bind
              source: ./build/reports
              target: /opt/worlds/build/test/reports
